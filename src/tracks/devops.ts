import type { Concept } from '../data';

export const devopsConcepts: Concept[] = [
  { id: 'shell', title: 'Command line', cat: 'Foundations', desc: 'Move around the filesystem, run commands, and read what they print back.', task: 'Create a folder for your service, cd into it, and list every file including hidden ones.', pre: [] },
  { id: 'http-basics', title: 'HTTP requests', cat: 'Networking', desc: 'Every call is a method, a path, a status code, headers, and a body.', task: 'Open your service in the browser and read the method, status, and content type in the Network tab.', pre: [] },
  { id: 'git-basics', title: 'Commits & history', cat: 'Git', desc: 'Save snapshots of your code with messages you will still understand later.', task: 'Init a repo in your service folder, commit the first working version, and read git log.', pre: [] },
  { id: 'yaml', title: 'YAML & JSON', cat: 'Foundations', desc: 'Read and write the two config formats that every tool below expects.', task: 'Write your service name, port, and two env vars as YAML, then as the same JSON object.', pre: [] },
  { id: 'run-service', title: 'Run it locally', cat: 'Foundations', desc: 'Install dependencies, start the process, and call it from a client.', task: 'Run npm ci and npm start, then curl GET / and confirm a 200 with a body.', pre: ['shell'] },
  { id: 'permissions', title: 'Files & permissions', cat: 'Foundations', desc: 'Read, write, execute: who is allowed to touch each file and why it matters.', task: 'Write a start.sh, make it executable with chmod, and run your service as a non-root user.', pre: ['shell'] },
  { id: 'ports', title: 'Ports & localhost', cat: 'Networking', desc: 'One process binds one port, and clients reach it by host plus port.', task: 'Start the service on port 3000, prove it is listening with lsof, then start a second copy and read the error.', pre: ['http-basics', 'shell'] },
  { id: 'dns', title: 'DNS names', cat: 'Networking', desc: 'Names resolve to addresses before any request leaves the machine.', task: 'Resolve github.com with dig, then point svc.local at 127.0.0.1 in /etc/hosts and open your service by name.', pre: ['http-basics', 'shell'] },
  { id: 'branching', title: 'Branches & merges', cat: 'Git', desc: 'Build a change in isolation, then fold it back into the main line.', task: 'Branch add-health, add a /health route, commit it, and merge the branch back into main.', pre: ['git-basics'] },
  { id: 'processes', title: 'Processes & signals', cat: 'Foundations', desc: 'A running program has a PID, an exit code, and a duty to react to signals.', task: 'Find your service PID, send SIGTERM, and make it close the HTTP server before it exits 0.', pre: ['run-service'] },
  { id: 'env-config', title: 'Config from the env', cat: 'Foundations', desc: 'Read settings from the environment so one build runs in every environment.', task: 'Make PORT and GREETING come from process.env, then start the service on 4000 without editing code.', pre: ['run-service'] },
  { id: 'unit-tests', title: 'Automated tests', cat: 'Delivery', desc: 'One command that proves the service still works, with no clicking involved.', task: 'Write a test asserting GET / returns 200 and wire it to npm test.', pre: ['run-service'] },
  { id: 'logs', title: 'Logs to stdout', cat: 'Operate', desc: 'Write one structured line per event to stdout and let the platform collect it.', task: 'Log every request as a single JSON line with method, path, status, and duration.', pre: ['run-service'] },
  { id: 'remote-repo', title: 'Push to GitHub', cat: 'Git', desc: 'Publish your history to a remote so other people and machines can fetch it.', task: 'Push main to a new GitHub repo and confirm your commits and branches appear there.', pre: ['branching'] },
  { id: 'image-build', title: 'Build an image', cat: 'Containers', desc: 'Freeze the service and its dependencies into one runnable artifact.', task: 'Write a Dockerfile on node:22-alpine that runs npm ci as a non-root user, then docker build it.', pre: ['run-service', 'permissions'] },
  { id: 'health-check', title: 'Health endpoint', cat: 'Operate', desc: 'Expose one cheap endpoint that says whether this instance can serve traffic.', task: 'Add /health returning 200, and make it return 503 as soon as SIGTERM starts the shutdown.', pre: ['processes', 'http-basics'] },
  { id: 'metrics', title: 'Expose metrics', cat: 'Operate', desc: 'Count and time things so you can see request rate, errors, and latency.', task: 'Serve /metrics with a request counter and a latency histogram, then read it after 20 requests.', pre: ['logs', 'http-basics'] },
  { id: 'pull-request', title: 'Pull requests', cat: 'Git', desc: 'Propose a change as a reviewable diff instead of pushing straight to main.', task: 'Open a pull request from a branch, review the diff line by line, then squash-merge it.', pre: ['remote-repo'] },
  { id: 'ci-pipeline', title: 'CI on every push', cat: 'Delivery', desc: 'A machine installs and tests every change before anyone trusts it.', task: 'Add a GitHub Actions workflow running npm ci and npm test, then break a test and watch it turn red.', pre: ['remote-repo', 'unit-tests', 'yaml'] },
  { id: 'container-run', title: 'Run a container', cat: 'Containers', desc: 'Start your image with published ports, injected env, and a clean stop.', task: 'Run your image with -p 8080:3000 and -e GREETING, curl it, then docker stop it and read the exit code.', pre: ['image-build', 'ports', 'processes'] },
  { id: 'image-layers', title: 'Layers & caching', cat: 'Containers', desc: 'Order the Dockerfile so a code change never rebuilds your dependencies.', task: 'Add .dockerignore and a multi-stage build, then compare image size and rebuild time before and after.', pre: ['image-build'] },
  { id: 'registry', title: 'Push to a registry', cat: 'Containers', desc: 'Store tagged images where every machine can pull the exact same bytes.', task: 'Tag your image with the commit sha and push it to GitHub Container Registry.', pre: ['image-build', 'remote-repo'] },
  { id: 'secrets', title: 'Secrets, not literals', cat: 'Delivery', desc: 'Keep credentials out of git and inject them at run time instead.', task: 'Move your registry token into a gitignored .env locally and a GitHub Actions secret in CI.', pre: ['env-config', 'ci-pipeline'] },
  { id: 'https-edge', title: 'HTTPS at the edge', cat: 'Networking', desc: 'A reverse proxy fronts your service, terminates TLS, and routes traffic.', task: 'Run Caddy in front of your container with a mkcert certificate and load https://svc.local without warnings.', pre: ['dns', 'container-run'] },
  { id: 'compose', title: 'Compose a stack', cat: 'Containers', desc: 'Declare several containers, their network, and their startup order in one file.', task: 'Write compose.yaml with your service plus Postgres and bring the whole stack up with one command.', pre: ['container-run', 'yaml'] },
  { id: 'dashboards', title: 'Dashboards', cat: 'Operate', desc: 'Collect metrics into a time series store and graph the few that matter.', task: 'Run Prometheus in a container scraping /metrics, then graph request rate and p95 latency.', pre: ['metrics', 'container-run'] },
  { id: 'cd-deploy', title: 'Deploy on merge', cat: 'Delivery', desc: 'Every merge to main ships the same artifact that CI already tested.', task: 'Add a deploy job on a self-hosted runner that pulls the new image tag and restarts your prod container.', pre: ['ci-pipeline', 'registry'] },
  { id: 'k8s-pods', title: 'Pods & manifests', cat: 'Containers', desc: 'Kubernetes runs containers from YAML you declare, not commands you type.', task: 'Create a kind cluster, apply a Pod manifest for your image, and port-forward to curl it.', pre: ['container-run', 'yaml'] },
  { id: 'iac', title: 'Infrastructure as code', cat: 'Delivery', desc: 'Declare the environment in files so you can apply and destroy it repeatably.', task: 'Declare your container and its network in Terraform with the Docker provider, then destroy and re-apply.', pre: ['compose', 'secrets'] },
  { id: 'rollback', title: 'Rollback fast', cat: 'Delivery', desc: 'Returning to the last good version is the safety net every deploy needs.', task: 'Ship a build with a broken /health, then redeploy the previous image tag and time the recovery.', pre: ['cd-deploy', 'health-check'] },
  { id: 'tracing', title: 'Distributed traces', cat: 'Operate', desc: 'Follow one request across services to see where its time actually went.', task: 'Add OpenTelemetry around your route and its Postgres call, then read one trace in Jaeger.', pre: ['metrics', 'compose'] },
  { id: 'alerts', title: 'Alerts worth waking for', cat: 'Operate', desc: 'Alert on user-visible symptoms at a threshold you would really act on.', task: 'Write a Prometheus rule that fires when the 5xx rate stays above 1% for five minutes, then trigger it.', pre: ['dashboards', 'health-check'] },
  { id: 'k8s-deploy', title: 'Deployments & probes', cat: 'Containers', desc: 'Replicas, readiness probes, and rolling updates instead of hand-run pods.', task: 'Replace the Pod with a two-replica Deployment plus a readiness probe and Service, then watch a rolling update.', pre: ['k8s-pods', 'health-check'] },
  { id: 'canary', title: 'Canary release', cat: 'Delivery', desc: 'Send a slice of traffic to the new version and watch it before going all in.', task: 'Route 10% of proxy traffic to a new container, compare its error rate, then shift the rest.', bonus: true, pre: ['cd-deploy', 'dashboards'] },
  { id: 'incident', title: 'Incident & postmortem', cat: 'Operate', desc: 'Run the outage calmly, then write down what you learned without blaming people.', task: 'Break prod on purpose, follow the alert to a rollback, and write a one-page timeline with two fixes.', pre: ['alerts', 'rollback'] },
  { id: 'gitops', title: 'GitOps', cat: 'Containers', desc: 'A controller inside the cluster keeps it synced to the manifests in git.', task: 'Install Argo CD in kind, point it at your manifests repo, and change replicas by merging a commit.', bonus: true, pre: ['k8s-deploy', 'cd-deploy'] },
  { id: 'service-mesh', title: 'Service mesh', cat: 'Containers', desc: 'Sidecars add mTLS, retries, and traffic telemetry without touching your code.', task: 'Install Linkerd in kind, inject it into your Deployment, and read the golden metrics it reports.', bonus: true, pre: ['k8s-deploy', 'tracing'] },
  { id: 'chaos', title: 'Chaos drill', cat: 'Operate', desc: 'Break things on purpose in a controlled window to learn what really fails.', task: 'Kill the Postgres container during load and confirm the alert fires and your service degrades cleanly.', bonus: true, pre: ['rollback', 'alerts'] },
];
