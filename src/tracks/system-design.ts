import type { Concept } from '../data';

export const systemDesignConcepts: Concept[] = [
  { id: 'client-server', title: 'Client-server model', cat: 'Foundations', desc: 'Separate the user-facing client from the server that stores data and applies rules.', task: 'Draw the photo app for 1,000 users as a phone, server, and database, then trace one upload request and response.', pre: [] },
  { id: 'workload-shape', title: 'Workload shape', cat: 'Foundations', desc: 'Describe a system by who uses it and how often they read and write.', task: 'For 1,000 photo-app users who open five feeds and upload one photo per day, calculate the daily reads and writes.', pre: [] },
  { id: 'latency-throughput', title: 'Latency vs throughput', cat: 'Foundations', desc: 'Distinguish how long one operation takes from how many operations finish over time.', task: 'Given 100 photo uploads completed in 20 seconds with a 600 ms average response time, calculate throughput and label the latency.', pre: [] },
  { id: 'failure-modes', title: 'Failure modes', cat: 'Reliability', desc: 'Expect components and networks to fail in different ways.', task: 'List what a photo-app user sees when the web server, image store, or database fails, and give one safe fallback for each.', pre: [] },
  { id: 'http-requests', title: 'HTTP requests', cat: 'Foundations', desc: 'Use HTTP methods, paths, headers, bodies, and status codes to exchange data.', task: 'Write the method, path, JSON body, and success status for a request that creates one photo in the 1,000-user app.', pre: ['client-server'] },
  { id: 'api-contracts', title: 'API contracts', cat: 'Foundations', desc: 'Define stable request and response shapes that clients can rely on.', task: 'Specify JSON requests and responses for uploading a photo, following a user, and liking a photo, including one error response each.', pre: ['client-server'] },
  { id: 'data-modeling', title: 'Data modeling', cat: 'Data & Storage', desc: 'Represent entities and relationships in structures that support required operations.', task: 'Sketch users, photos, follows, and likes for the photo app, then mark each identifier and relationship.', pre: ['workload-shape'] },
  { id: 'back-of-envelope', title: 'Back-of-envelope', cat: 'Foundations', desc: 'Estimate traffic, storage, and bandwidth with simple assumptions.', task: 'For 100,000 users uploading two 4 MB photos per day, calculate average uploads per second and storage added per day and year.', pre: ['workload-shape', 'latency-throughput'] },
  { id: 'scaling-up-out', title: 'Vertical vs horizontal', cat: 'Traffic & Scale', desc: 'Compare making one machine stronger with adding more machines.', task: 'Compare one photo server with four times the capacity against four equal servers, then list how each handles one server failure.', pre: ['workload-shape'] },
  { id: 'redundancy', title: 'Redundancy', cat: 'Reliability', desc: 'Keep spare components or copies so one failure does not stop the service.', task: 'Redraw the 1,000-user photo app with two servers and two image copies, then cross out one component and trace a surviving request path.', pre: ['failure-modes'] },
  { id: 'load-balancing', title: 'Load balancing', cat: 'Traffic & Scale', desc: 'Spread incoming requests across healthy servers.', task: 'Distribute 12 photo-feed requests round-robin across three servers and verify that each server receives four.', pre: ['scaling-up-out', 'redundancy'] },
  { id: 'caching-layers', title: 'Caching layers', cat: 'Traffic & Scale', desc: 'Keep reusable data closer to callers to reduce repeated work.', task: 'Design a cache entry for a user feed, choose a TTL, and show what happens on the first read, second read, and a new upload.', pre: ['http-requests', 'latency-throughput'] },
  { id: 'sql-vs-nosql', title: 'SQL vs NoSQL', cat: 'Data & Storage', desc: 'Choose storage based on relationships, access patterns, scale, and guarantees.', task: 'Choose SQL or NoSQL for photo metadata, follow relationships, and feed items, then give one workload-based reason for each choice.', pre: ['data-modeling'] },
  { id: 'capacity-planning', title: 'Capacity planning', cat: 'Traffic & Scale', desc: 'Turn workload estimates into the resources needed for expected peaks.', task: 'For 1 million users opening ten feeds per day, calculate average and 10-times-peak requests per second and the servers needed at 500 requests per second each.', pre: ['back-of-envelope', 'redundancy'] },
  { id: 'observability', title: 'Observability', cat: 'Reliability', desc: 'Use metrics, logs, and traces to understand a running system.', task: 'Define one metric, log field, and trace span that would reveal why photo uploads became slow, then state the expected healthy value for each.', pre: ['api-contracts', 'failure-modes'] },
  { id: 'monolith-services', title: 'Monolith vs services', cat: 'Architecture', desc: 'Choose whether capabilities deploy together or behind separate service boundaries.', task: 'Draw the photo app first as one deployable and then as upload, feed, and social services, marking every network call added by the split.', pre: ['api-contracts', 'data-modeling'] },
  { id: 'cdn', title: 'CDN', cat: 'Traffic & Scale', desc: 'Serve static content from edge locations near users.', task: 'Draw a viewer, CDN edge, and image origin, then trace a cache miss and hit for the same photo and count the origin requests.', pre: ['caching-layers'] },
  { id: 'rate-limiting', title: 'Rate limiting', cat: 'Traffic & Scale', desc: 'Bound how quickly a caller can consume shared capacity.', task: 'Apply a full 60-token bucket with a one-token-per-second refill to an instant burst of 75 like requests and calculate accepted and rejected requests.', pre: ['api-contracts', 'capacity-planning'] },
  { id: 'database-indexes', title: 'Database indexes', cat: 'Data & Storage', desc: 'Maintain ordered lookup structures that speed reads at a write and storage cost.', task: "Propose column order for indexes that fetch a user's newest photos and a photo's likes, then write the query each index supports.", pre: ['sql-vs-nosql'] },
  { id: 'replication', title: 'Replication', cat: 'Data & Storage', desc: 'Copy data across nodes to improve availability and read capacity.', task: 'Draw one primary and two replicas for the 10-million-user photo app, route an upload and two feed reads, then show one stale-read outcome.', pre: ['sql-vs-nosql', 'redundancy'] },
  { id: 'table-partitioning', title: 'Partitioning', cat: 'Data & Storage', desc: 'Split a large dataset into smaller pieces that remain within one logical store.', task: 'Partition photo records by upload month and place three example photos dated January, February, and March into their matching partitions.', pre: ['data-modeling', 'capacity-planning'] },
  { id: 'message-queues', title: 'Message queues', cat: 'Architecture', desc: 'Buffer work so producers and consumers can run at different speeds.', task: 'Draw upload service, thumbnail queue, and two workers, then define the message fields and mark when a message is acknowledged.', pre: ['monolith-services', 'load-balancing'] },
  { id: 'health-checks', title: 'Health checks', cat: 'Reliability', desc: 'Test whether an instance can start, run, and receive traffic safely.', task: 'Define startup, liveness, and readiness checks for a photo server, then decide which failures should remove it from the load balancer.', pre: ['observability', 'load-balancing'] },
  { id: 'sharding', title: 'Sharding', cat: 'Data & Storage', desc: 'Distribute one logical dataset across independent database nodes.', task: 'Assign users 17, 42, and 99 to 16 photo-metadata shards using user ID modulo 16, then identify the shard for each.', pre: ['table-partitioning', 'database-indexes'] },
  { id: 'consistency-models', title: 'Consistency models', cat: 'Data & Storage', desc: 'Define when readers are guaranteed to observe a completed write.', task: 'Draw a like written at time zero and replicated two seconds later, then show the count seen under strong and eventual consistency at one second.', pre: ['replication'] },
  { id: 'async-processing', title: 'Async processing', cat: 'Architecture', desc: "Move slow or deferrable work outside the user's request path.", task: 'Draw an upload timeline that returns before thumbnail creation, then mark the synchronous response, queued work, retry, and final completion.', pre: ['message-queues'] },
  { id: 'load-shedding', title: 'Load shedding', cat: 'Traffic & Scale', desc: 'Reject lower-priority work when accepting everything would collapse the system.', task: 'When a feed service that handles 1,000 requests per second receives 1,400, choose 400 requests to reject and verify that uploads remain below capacity.', pre: ['rate-limiting', 'health-checks'] },
  { id: 'recovery-drills', title: 'Recovery drills', cat: 'Reliability', desc: 'Practice restoring service and data before a real incident occurs.', task: 'Write a timed drill for a failed photo database primary covering detection, promotion, reconnection, and verification, with target RTO and RPO values.', pre: ['replication', 'health-checks'] },
  { id: 'api-gateway', title: 'API gateway', cat: 'Architecture', desc: 'Give clients one entry point for routing and shared request policies.', task: 'Draw mobile and web clients entering one gateway that authenticates and routes upload, feed, and like requests to the correct services.', pre: ['monolith-services', 'rate-limiting'] },
  { id: 'idempotency', title: 'Idempotency', cat: 'Reliability', desc: 'Make repeated delivery of the same operation produce one intended result.', task: 'Trace three retries of one photo upload using the same idempotency key and show the stored photo count and response returned after each attempt.', pre: ['async-processing', 'api-contracts'] },
  { id: 'cap-tradeoffs', title: 'CAP trade-offs', cat: 'Data & Storage', desc: 'Choose between consistent and available responses during a network partition.', task: 'During a split between two photo-app regions, choose whether follow creation and like counting favor consistency or availability and complete a two-row decision table.', pre: ['consistency-models', 'sharding'] },
  { id: 'event-driven-design', title: 'Event-driven design', cat: 'Architecture', desc: 'Let services react to immutable facts without direct coordination.', task: 'Define PhotoUploaded, FollowCreated, and PhotoLiked events, then map each publisher and subscriber for feeds, notifications, and analytics.', pre: ['async-processing', 'monolith-services'] },
  { id: 'geo-distribution', title: 'Geo-distribution', cat: 'Reliability', desc: 'Place data and computation across regions to reduce latency and survive regional failure.', task: 'For 100 million users across North America, Europe, and Asia, draw request routing and data placement, then trace reads and writes during a European outage.', bonus: true, pre: ['cdn', 'consistency-models', 'recovery-drills'] },
  { id: 'consensus', title: 'Consensus', cat: 'Data & Storage', desc: 'Have distributed nodes agree on one value despite failures and delays.', task: 'For five photo-metadata replicas, calculate the majority quorum and test whether writes can continue after zero, one, two, and three replica failures.', bonus: true, pre: ['cap-tradeoffs', 'recovery-drills'] },
  { id: 'event-sourcing', title: 'Event sourcing', cat: 'Architecture', desc: 'Store state changes as an ordered event log and rebuild current state from it.', task: 'Write six upload, like, and unlike events for one photo, then replay them in order to calculate its final status and like count.', bonus: true, pre: ['event-driven-design', 'idempotency'] },
  { id: 'cell-architecture', title: 'Cell-based architecture', cat: 'Architecture', desc: 'Divide a large service into isolated copies so failures affect only one slice of users.', task: 'Split 100 million photo-app users across 20 cells by user ID, draw the shared router, and calculate the affected users and percentage when one cell fails.', bonus: true, pre: ['geo-distribution', 'load-shedding', 'api-gateway'] },
];
